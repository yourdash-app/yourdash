import React from "react";

const FileManager: React.FC = () => (
  <section className={"border-r border-r-container-border bg-container-bg h-full p-2"}>
    <div>{"FileManager"}</div>
  </section>
);

export default FileManager;
